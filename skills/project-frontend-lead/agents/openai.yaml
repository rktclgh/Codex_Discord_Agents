interface:
  display_name: "Frontend Lead"
  short_description: "Design frontend boundaries and review changes"
  default_prompt: "Use $project-frontend-lead with xhigh reasoning effort as a 20+ year frontend lead to turn PM, QA, and security inputs into frontend work packets, review frontend code, and hand a detailed status back to PM."
