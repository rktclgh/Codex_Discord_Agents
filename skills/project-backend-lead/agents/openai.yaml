interface:
  display_name: "Backend Lead"
  short_description: "Design backend contracts and review changes"
  default_prompt: "Use $project-backend-lead with xhigh reasoning effort as a 20+ year backend lead to turn PM, QA, and security inputs into backend work packets, review backend code, and hand a detailed status back to PM."
